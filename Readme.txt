-Make a virtual environment using virtualenv

virtualenv db_env

-Install requirements from requirements.txt

pip install -r requirements.txt

Make a DataBase in MongoDB with the name "faculty_auth".
Then make a collection named "User_Auth" in this database.

To run flask server:
python3 flask_server.py
